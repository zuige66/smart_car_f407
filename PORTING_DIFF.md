# 移植后的差异说明

本文档说明 `clion_car` 迁移到 `smart_car` 后，硬件、外设、代码结构和实现方式上的主要变化。

## 1. 平台变化

| 项目 | 旧工程 | 新工程 |
|---|---|---|
| 主控 | `STM32F103C8T6` | `STM32F407VET6` |
| 内核 | Cortex-M3 | Cortex-M4 |
| 主频 | `72 MHz` | `168 MHz` |
| 外部晶振 | `8 MHz` | `25 MHz` |
| 资源规模 | 较小 | 更大 |
| HAL 时基 | F103 工程默认方案 | `TIM8` |

## 2. 构建与工程变化

| 项目 | 旧工程 | 新工程 |
|---|---|---|
| 工程名 | `clion_car` | `smart_car` |
| 工具链目标 | ARM GCC | ARM GCC |
| 本次实际验证 | 未在本轮重做旧工程验证 | 已用 `cmake --preset Debug` 和 `cmake --build --preset Debug` 验证通过 |
| RTOS | FreeRTOS CMSIS-RTOS2 | FreeRTOS CMSIS-RTOS2 |

## 3. 外设映射变化

### 3.1 OLED

- 旧工程：
  - OLED 驱动逻辑是按旧工程环境写的
  - 实际相当于走旧 `I2C1`

- 新工程：
  - OLED 已明确改到 `I2C3`
  - 引脚变为：
    - `PA8 = SCL`
    - `PC9 = SDA`

也就是说：

- 从：旧 I2C 习惯用法
- 变成：当前板子的 `I2C3`

### 3.2 MLX90614

- 旧工程：
  - `I2C1`
  - `PB6/PB7`

- 新工程：
  - 仍然是 `I2C1`
  - 仍然是 `PB6/PB7`

这一部分基本没变。

### 3.3 调试串口

- 旧工程：
  - `USART2`
  - `PA2/PA3`

- 新工程：
  - 仍然是 `USART2`
  - 仍然是 `PA2/PA3`

这部分逻辑保持一致。

### 3.4 WiFi 串口

- 旧工程：
  - WiFi 只是规划接口，基本未完成

- 新工程：
  - `USART3`
  - `PC10/PC11`
  - 软件层接口仍保留，但功能仍是占位实现

变化是：

- 从：规划中
- 变成：硬件映射已经明确，软件接口已保留

### 3.5 MQ8 气体传感器

- 旧工程：
  - `MQ8_DO` 和 ADC 逻辑按 F103 方案设计
  - AO 的实际 ADC 接法依赖旧工程配置

- 新工程：
  - `MG8_DO = PC13`
  - `MG8_AO = PC1`
  - `ADC1 Channel 11` 已真实接入读取 AO

变化是：

- 从：旧板 ADC 假设
- 变成：新板 `ADC1 + PC1` 真实接入

也可以理解为：

- 新工程比刚迁移时多了真正的 AO 采样能力

### 3.6 超声模块

- 旧工程：
  - `HCSR04` 依赖旧板上的定时器输入捕获思路

- 新工程：
  - `Trig = PB8`
  - `Echo = PB9`
  - `TIM1` 配成 `1 MHz` 计时基准
  - 当前实现方式为：
    - `GPIO 轮询`
    - `TIM1 微秒计时`

变化是：

- 从：旧输入捕获方案
- 变成：`TIM1 + GPIO` 的软件测距方案

原因是：

- 当前 `Echo` 没有直接按输入捕获通道映射到 `TIM1`
- 所以没有继续沿用旧工程的捕获实现

### 3.7 电机 PWM

- 旧工程：
  - PWM 使用 `TIM2`
  - 引脚是 `PA0/PA1`

- 新工程：
  - PWM 改为 `TIM4`
  - 引脚是 `PD13/PD14`

这是迁移里最重要的外设变化之一。

## 4. GPIO 命名与控制变化

### 4.1 循迹传感器

- 旧工程：
  - `X1/X2/X3/X4` 已存在

- 新工程：
  - 逻辑上仍保留 `X1/X2/X3/X4`
  - 新映射为：
    - `X1 = PA0`
    - `X2 = PA1`
    - `X3 = PD1`
    - `X4 = PD2`

变化是：

- 从：旧板引脚布局
- 变成：新板对应的输入引脚

### 4.2 蜂鸣器

- 旧工程：
  - 使用旧工程中的蜂鸣器命名和控制极性

- 新工程：
  - `BUZZER_IN = PE5`
  - 已通过兼容层统一控制开关极性

变化是：

- 从：任务代码直接写硬件
- 变成：通过兼容层统一管理

### 4.3 电机方向控制

- 旧工程：
  - 方向引脚比较直接：
    - `AIN1`
    - `AIN2`
    - `BIN1`
    - `BIN2`

- 新工程：
  - 命名拆成了更多左右侧信号：
    - `AIN1_L`
    - `AIN2_L`
    - `AIN1_R`
    - `AIN2_R`
    - `BIN1_L`
    - `BIN2_L`
    - `BIN2_R`
    - `STBY_L`
    - `STBY_R`

当前迁移代码实际采用：

- 左方向：`AIN1_L + AIN2_L`
- 右方向：`AIN1_R + AIN2_R`
- 使能：`STBY_L + STBY_R`

所以这里的变化不是简单“替换引脚”，而是：

- 从：旧工程四个标准方向脚
- 变成：新工程一组更复杂、且仍需上板确认的命名体系

这也是当前最可能需要你再手动确认的部分。

## 5. 软件结构变化

### 5.1 应用层目录

- 旧工程：
  - 应用代码位于 `clion_car/Core/App/Tasks`

- 新工程：
  - 应用代码现在位于 `smart_car/Core/App/Tasks`

变化是：

- 从：旧工程任务目录
- 变成：新工程独立任务目录

### 5.2 新增兼容层

- 旧工程：
  - 应用代码直接依赖 F103 的引脚名和外设名

- 新工程：
  - 新增了 `Core/App/Tasks/board_compat.h`

兼容层当前负责：

- 统一 UART 选择
- 统一 I2C 设备选择
- 统一 PWM 定时器/通道映射
- 统一蜂鸣器和待机控制
- 标记哪些功能仍是占位

变化是：

- 从：任务代码直接写死底层硬件
- 变成：增加一层板级适配

这是这次移植里比较重要的结构优化。

### 5.3 FreeRTOS 接入方式

- 旧工程：
  - 原本已有任务、队列、信号量框架

- 新工程：
  - 已在 `freertos.c` 中重新接好任务入口
  - `MotorAction` 队列大小改成和 `sizeof(MotorCmd_t)` 对齐
  - `LedTask` 等入口方式与当前代码保持一致

变化是：

- 从：旧工程任务注册
- 变成：新工程中重新接线后的任务注册

### 5.4 中断处理

- 旧工程：
  - 中断体系按 F103 工程生成

- 新工程：
  - 补上了 `USART2_IRQHandler`
  - 补上了 `USART3_IRQHandler`
  - 补上了 `TIM8_UP_TIM13_IRQHandler`
  - 修正了原本时间基中断不一致的问题

变化是：

- 从：旧工程中断布局
- 变成：适配 F407 的中断布局

## 6. 功能层面的变化

### 6.1 已迁移并能工作的部分

以下功能已经迁移并接入：

- 主控制任务框架
- 电机驱动框架
- 循迹控制
- 避障状态机
- 温度状态机
- 串口命令处理
- OLED 显示
- MQ8 的 AO/DO 读取
- 超声测距主链路

### 6.2 移植后仍是占位的部分

以下模块在新工程中仍然不是完整实现：

- 电池管理
- WiFi 通信协议
- RFID 读卡
- 编码器反馈
- 状态灯支持

所以从功能完整度看：

- 不是“所有旧工程设想功能都完整落地”
- 而是“核心控制链路先迁过去并跑通，非核心功能保留接口”

## 7. 总结

这次移植不是简单拷贝代码，而是做了下面这些变化：

- 主控从 `F103` 变成 `F407`
- 主频从 `72 MHz` 变成 `168 MHz`
- 电机 PWM 从 `TIM2` 变成 `TIM4`
- OLED 改到 `I2C3`
- MQ8 的 AO 新增了真实 `ADC1` 采样
- 超声从旧输入捕获思路改成 `TIM1 + GPIO` 方案
- 新增板级兼容层
- 新工程已完成 ARM GCC 编译验证

当前最值得你上板重点确认的是：

- 电机方向控制引脚是否与软件映射完全一致
